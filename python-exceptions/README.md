# Safe Print List

## Description
This project contains a Python function that prints elements of a list safely.

The function prints up to `x` elements of a list on the same line, followed by a new line. It returns the **number of elements actually printed**.

## Prototype
```python
def safe_print_list(my_list=[], x=0):
