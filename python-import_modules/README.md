# Python Import Project

This project demonstrates how to import a function from another Python file.

## Files

- `add_0.py` – contains the function `add(a, b)` that returns the sum of two numbers.
- `0-add.py` – imports the `add` function and prints the result of `1 + 2`.

## Example Output

1 + 2 = 3
